name: demo_suite
matrix:
  nodes: [1]
  ranks_per_node: [1, 2]
  params:
    input_size: [100, 200]
resources:
  threads: 1
  gpus: 0
command: "echo 'Task ${task_id}: Running with ${ranks_per_node} ranks on input ${input_size}'"
