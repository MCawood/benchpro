name: lammps_melt
requirements:
  code: lammps
  version: "2023.08.02"
  build_label: "cmake_mpi"

matrix:
  params:
    x: [10, 20]
    y: [10, 20]
    z: [10]

resources:
  threads: 1

# Copy input script to task dir and run
command: "cp /Users/mcawood/dev/benchpro_antigravity/examples/build/in.melt . && lmp -in in.melt -var x ${x} -var y ${y} -var z ${z}"
