name: pi_benchmark
requirements:
  code: pi_calc
  version: "1.0"
  build_label: "gcc_opt"

matrix:
  params:
    iterations: [1000000, 10000000, 100000000]

resources:
  threads: 1

command: "pi_calc ${iterations}"
