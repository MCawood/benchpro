# Core dependencies
click>=8.0.0
pyyaml>=6.0
jinja2>=3.0.0
pydantic>=2.0.0
tabulate>=0.9.0

# Database dependencies for new registry system
# Note: sqlite3 is built into Python, but we may need these for advanced features
sqlalchemy>=2.0.0  # Database ORM for complex queries and connection management
alembic>=1.12.0    # Database migration tool

# Development dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
black>=23.0.0
isort>=5.0.0
flake8>=6.0.0
mypy>=1.0.0
sphinx>=6.0.0
sphinx-rtd-theme>=1.2.0 